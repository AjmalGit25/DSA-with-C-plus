int directionCheck (vector<vector<int>> & mat) {
    
    int rows = mat.size();
	int cols = mat[0].size();

	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {

			if (this element) {
				// upward
				for (int r = i - 1; r >= 0; r--) {

				}

				// downward
				for (int r = i + 1; r < rows; r++) {

				}

				// left
				for (int c = j - 1; c >= 0; c--) {

				}

				// right
				for (int c = j + 1; c < cols; c++) {

				}
			}
		}
	}
}